import { createStore } from 'vuex'

import {MediaModule} from "./modules/media-module";


export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    MediaModule
  }
})
